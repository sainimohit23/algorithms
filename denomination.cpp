#include <iostream>
using namespace std;
#include <vector>

int denominations(int total,int index){
	if(total == 0) return 0;

	sum == 0;
	while(sum <= total){
		
	}
}

int main(){
	int n;
	cin>>n;

	vector<int> d;

	for(int i=0;i<n;++i){
		int x; cin>>x;
		v.push_back(x);
	}

	int total;
	cin>>total;

	int num = denominations(total,n-1);
}